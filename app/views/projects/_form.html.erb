<%= form_for(@project) do |f| %>
<%= f.error_messages %>
<div class="field">
	<%= f.label :name, "项目名" %><br />
	<%= f.text_field :name %>
</div>

<div class="actions">
	<%= f.submit %>
</div>

<% end %>